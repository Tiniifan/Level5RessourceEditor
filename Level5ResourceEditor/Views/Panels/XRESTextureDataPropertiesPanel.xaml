<UserControl x:Class="Level5ResourceEditor.Views.Panels.XRESTextureDataPropertiesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <TextBlock Text="Name" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk1" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk2" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk3" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk3, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk4" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk4, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk5" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk5, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>

            <TextBlock Text="Unk6" 
                      Style="{StaticResource ImaginationLabel}"
                      Margin="0,0,0,5"/>
            <TextBox Style="{StaticResource ImaginationTextBox}"
                    Text="{Binding Unk6, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Margin="0,0,0,15"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>